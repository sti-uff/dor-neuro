<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Dorneuro</title>
    <%= stylesheet_link_tag 'bootstrap', 'style', 'jquery-ui-1.8.21.custom', 'token-input', 'token-input-facebook' %>
    <%= javascript_include_tag :defaults, 'jquery-ui-1.8.21.custom.min', 'jquery.ui.datepicker-pt-BR', 'bootstrap' %>
    <%= csrf_meta_tag %>
    <%= yield :head %>
  </head>
  <body>
    
      <%= render 'layouts/menu' %>
    
      <% flash.each do |name, msg| %>
        <% unless msg.empty? %>
          <%= content_tag :div, :id => "flash_#{name}", :class => "alert alert-#{name}" do %>
            <%= link_to 'x', '#', :class => 'close', 'data-dismiss' => 'alert' %>
            <%= msg %>
          <% end %>
        <% end %>
      <% end %>
  
      <div class="container">
        <%= yield %>
      </div>

  </body>
</html>
