<h1>Verificar CHEPS</h1>

<%= render 'partials/escolher_voluntario', :usuario => false %>

<% if @cheps %>
  <div>
    <%= exibir_pdf_ou_imagem(@cheps)%>
  </div>
<% else %>
  <p>O CHEPS deste voluntário não foi registrado.</p>
<% end %>

<%= link_to 'Voltar', show_neurofisiologia_path, :class => 'btn' %>